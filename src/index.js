import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import BoardApp from './components/ui/BoardApp';

ReactDOM.render(
  <React.StrictMode>
    <BoardApp />
  </React.StrictMode>,
  document.getElementById('root')
);
